<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.li.demo">

    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <!-- 网络 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CatchExceptionTest.CrashApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".testTouch.TouchActivity" />
        <activity android:name=".testThreadpool.ThreadPoolActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RecyclerViewDemo.RecyclerViewActivity" />
        <activity android:name=".GlideTest.GlideActivity" />
        <activity android:name=".CheckBoxTest.SingleCheckActivity" />
        <activity android:name=".testComAdapter.ComAdapterAty" />
        <activity android:name=".CatchExceptionTest.CatchExceptionLogActivity" />
        <activity android:name=".view.xiuyixiu.RadarActivity" />
        <activity android:name=".view.threeDView.ThreeDActivity" />
        <activity android:name=".view.threeDView.WeatherActivity" />
        <activity android:name=".rxjavaTest.RxjavaActivity" />
        <activity android:name=".RecyclerViewDemo.CardLayoutManager.CardLayoutActivity" />
        <activity android:name=".view.toutiao.ScrollTopActivity" />
        <activity android:name=".view.RecyclerIndicatorView.MoreTabActivity" />
        <activity android:name=".view.RecyclerIndicatorView.BannerActivity" />
        <activity android:name=".tab.MoreTab2Activity" />
        <activity android:name=".tab.TabMainActivity" />
        <activity android:name=".MultiLevel.MultiLevelActivity"></activity>
    </application>

</manifest>